# data-service (VM) — port 8001
# SQLite file location (kept in the separate data/ folder)
DB_PATH=../data/student.db
# Shared secret the backend must present. MUST match backend's API_KEY.
API_KEY=dev-shared-key-change-me