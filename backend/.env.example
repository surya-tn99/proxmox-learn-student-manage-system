# backend (LXC 1) — port 8000
# URL of the data-service running on the VM
DB_API_URL=http://127.0.0.1:8001
# Shared secret. MUST match data-service's API_KEY.
API_KEY=dev-shared-key-change-me
# Secret used to sign login tokens (HMAC)
SECRET_KEY=dev-secret-change-me